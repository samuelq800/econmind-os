"use client";import{createContext,useContext,useEffect,useMemo,useState}from"react";type V={theme:"light"|"dark";toggleTheme:()=>void;ready:boolean};const C=createContext<V|null>(null);export function ThemeProvider({children}:{children:React.ReactNode}){const[theme,setTheme]=useState<"light"|"dark">("light"),[ready,setReady]=useState(false);useEffect(()=>{let active=true;queueMicrotask(()=>{if(!active)return;const stored=localStorage.getItem("econmind-theme"),preferred=matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light";setTheme(stored==="dark"||stored==="light"?stored:preferred);setReady(true)});return()=>{active=false}},[]);useEffect(()=>{if(!ready)return;document.documentElement.classList.toggle("dark",theme==="dark");localStorage.setItem("econmind-theme",theme)},[ready,theme]);const value=useMemo(()=>({theme,ready,toggleTheme:()=>setTheme(t=>t==="light"?"dark":"light")}),[ready,theme]);return<C.Provider value={value}>{children}</C.Provider>}export function useTheme(){const c=useContext(C);if(!c)throw new Error("ThemeProvider missing");return c}
