import type{ModelParameter}from"@/lib/economics/types";export function ParameterControl({parameter,value,onChange}:{parameter:ModelParameter;value:number;onChange:(v:number)=>void}){const commit=(n:number)=>onChange(Math.min(parameter.max,Math.max(parameter.min,Number.isFinite(n)?n:parameter.defaultValue)));return<div className="border-b border-[var(--line)] py-4 last:border-0"><div className="mb-3 flex items-start justify-between gap-3"><div><label htmlFor={parameter.id} className="text-xs font-bold">{parameter.label} <span className="font-mono font-normal text-[var(--ink-faint)]">({parameter.symbol})</span></label><p className="mt-1 text-[11px] leading-4 text-[var(--ink-muted)]">{parameter.description}</p></div><input aria-label={`${parameter.label} number input`} type="number" min={parameter.min} max={parameter.max} step={parameter.step} value={value} onChange={e=>commit(Number(e.target.value))} className="h-8 w-20 rounded-md border border-[var(--line)] bg-[var(--canvas)] px-2 text-right text-xs font-semibold tabular-nums outline-none focus:border-[var(--accent)]"/></div><div className="flex items-center gap-3"><span className="w-7 text-[9px] text-[var(--ink-faint)]">{parameter.min}</span><input id={parameter.id} type="range" min={parameter.min} max={parameter.max} step={parameter.step} value={value} onChange={e=>commit(Number(e.target.value))} className="min-w-0 flex-1"/><span className="w-7 text-right text-[9px] text-[var(--ink-faint)]">{parameter.max}</span></div></div>}
