"use client";import{BookmarkPlus,GitCompareArrows}from"lucide-react";import{usePersistentState}from"@/lib/hooks/use-persistent-state";import{safePercentChange,type ScenarioSnapshot}from"@/lib/economics/types";import{Button}from"@/components/ui/button";const pretty=(k:string)=>k.replace(/([A-Z])/g," $1").replace(/^./,c=>c.toUpperCase()),format=(v:number)=>new Intl.NumberFormat("en-US",{maximumFractionDigits:2}).format(v);export function ScenarioComparison({storageKey,parameters,results,metrics}:{storageKey:string;parameters:Record<string,number>;results:Record<string,number>;metrics:string[]}){const[s,setS]=usePersistentState<{A:ScenarioSnapshot|null;B:ScenarioSnapshot|null}>(storageKey,{A:null,B:null}),save=(label:"A"|"B")=>setS(c=>({...c,[label]:{label,parameters,results,savedAt:new Date().toISOString()}}));return<section className="rounded-xl border border-[var(--line)] bg-[var(--surface)] p-5 sm:p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.12em]"><GitCompareArrows size={16}/><h2>Scenario comparison</h2></div><p className="mt-1 text-[11px] text-[var(--ink-muted)]">Capture the current state, adjust parameters, then compare. Saved only in this browser.</p></div><div className="flex gap-2"><Button variant="secondary" size="sm" onClick={()=>save("A")}><BookmarkPlus size={14}/>Save A</Button><Button variant="secondary" size="sm" onClick={()=>save("B")}><BookmarkPlus size={14}/>Save B</Button></div></div><div className="mt-5 overflow-hidden rounded-lg border border-[var(--line)]"><div className="grid grid-cols-[1.3fr_1fr_1fr_1fr] bg-[var(--surface-subtle)] px-3 py-2 text-[9px] font-bold uppercase tracking-wider text-[var(--ink-faint)]"><span>Metric</span><span className="text-right">Scenario A</span><span className="text-right">Scenario B</span><span className="text-right">Change</span></div>{metrics.map(m=>{const a=s.A?.results[m],b=s.B?.results[m],change=a!==undefined&&b!==undefined?safePercentChange(a,b):null;return<div key={m} className="grid grid-cols-[1.3fr_1fr_1fr_1fr] border-t border-[var(--line)] px-3 py-3 text-[11px]"><span className="truncate font-semibold">{pretty(m)}</span><span className="text-right text-[var(--ink-muted)]">{a===undefined?"—":format(a)}</span><span className="text-right text-[var(--ink-muted)]">{b===undefined?"—":format(b)}</span><span className={`text-right font-semibold ${change===null?"text-[var(--ink-faint)]":change>0?"text-[var(--accent)]":change<0?"text-[var(--red)]":"text-[var(--ink-muted)]"}`}>{change===null?"—":`${change>0?"+":""}${format(change)}%`}</span></div>})}</div></section>}
