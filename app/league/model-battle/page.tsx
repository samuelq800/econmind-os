import Link from "next/link";
import { ArrowRight, Scale } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
const models = [{ title: "Competitive Labour Market", assumptions: "Many employers compete for workers; wages reflect marginal productivity.", effects: "A substantial minimum wage can reduce labour demanded when set above equilibrium.", strengths: "Makes wage floors, labour demand and unemployment channels explicit.", limits: "Can understate employer wage-setting power." }, { title: "Monopsony Labour Market", assumptions: "Employers have wage-setting power because labour supply to each firm is imperfectly elastic.", effects: "A moderate minimum wage can raise both wages and employment up to a point.", strengths: "Explains why low pay may coexist with employer market power.", limits: "Requires evidence about local hiring power and labour mobility." }];
export default function ModelBattlePage() { return <main className="mx-auto min-h-screen max-w-6xl px-5 py-12 sm:px-8"><Badge className="border-[var(--blue)] bg-[var(--blue-soft)] text-[var(--blue)]">Pilot Ready</Badge><header className="mt-5 max-w-3xl"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--accent)]">Model Battle</p><h1 className="mt-3 text-5xl font-bold tracking-[-.06em]">Should governments substantially increase the minimum wage?</h1><p className="mt-5 text-sm leading-7 text-[var(--ink-muted)]">This pilot-ready format asks teams to make assumptions visible before debating predictions.</p></header><section className="mt-10 grid gap-5 lg:grid-cols-2">{models.map((model) => <Card key={model.title} className="p-6"><div className="flex items-center gap-3"><Scale className="text-[var(--accent)]" size={20} /><h2 className="text-xl font-bold">{model.title}</h2></div><dl className="mt-6 space-y-5 text-sm leading-6"><div><dt className="font-bold">Assumptions</dt><dd className="mt-1 text-[var(--ink-muted)]">{model.assumptions}</dd></div><div><dt className="font-bold">Predicted effects</dt><dd className="mt-1 text-[var(--ink-muted)]">{model.effects}</dd></div><div><dt className="font-bold">Strengths</dt><dd className="mt-1 text-[var(--ink-muted)]">{model.strengths}</dd></div><div><dt className="font-bold">Limitations</dt><dd className="mt-1 text-[var(--ink-muted)]">{model.limits}</dd></div></dl></Card>)}</section><Card className="mt-8 p-6"><h2 className="text-lg font-bold">Structured comparison framework</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{["Which assumptions fit the case?", "What evidence would distinguish the models?", "Who gains and who loses?", "What are the limits of each model?"].map((prompt, index) => <div key={prompt} className="rounded-lg bg-[var(--surface-subtle)] p-4 text-sm"><span className="font-bold text-[var(--accent)]">0{index + 1}</span><p className="mt-3 font-semibold">{prompt}</p></div>)}</div><Link href="/league" className="mt-6 inline-flex items-center gap-2 text-xs font-bold text-[var(--accent)]">Back to League <ArrowRight size={14} /></Link></Card></main>; }
