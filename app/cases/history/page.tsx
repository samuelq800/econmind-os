"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { FileText, Trash2 } from "lucide-react";
import { useAuth } from "@/components/auth/auth-provider";
import { Button } from "@/components/ui/button";
import { deleteCaseRun, listMyCaseRuns, type CaseRunRow } from "@/lib/supabase/cases";

export default function CaseHistoryPage() { const { user, openAuth } = useAuth(); const [rows, setRows] = useState<CaseRunRow[]>([]); const [error, setError] = useState(""); const load = () => { if (user) void listMyCaseRuns().then(setRows).catch((caught) => setError(caught instanceof Error ? caught.message : "Could not load case history.")); }; useEffect(load, [user]); if (!user) return <main className="mx-auto grid min-h-[65vh] max-w-xl place-items-center px-5 text-center"><div><h1 className="text-3xl font-bold">Sign in to view saved cases</h1><p className="mt-3 text-sm text-[var(--ink-muted)]">Guest work stays in this browser. Saved case records are private to your account.</p><Button className="mt-5" onClick={() => openAuth("sign-in")}>Sign in</Button></div></main>; return <main className="mx-auto min-h-screen max-w-5xl px-5 py-12 sm:px-8"><header><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--accent)]">Private records</p><h1 className="mt-2 text-4xl font-bold">Case history</h1><p className="mt-3 text-sm text-[var(--ink-muted)]">Only you can view or remove these saved records.</p></header>{error && <p className="mt-5 rounded-lg bg-[var(--red-soft)] p-4 text-sm text-[var(--red)]">{error}</p>}<div className="mt-8 space-y-3">{rows.map((row) => <article key={row.id} className="flex items-center justify-between gap-4 rounded-xl border border-[var(--line)] bg-[var(--surface)] p-5"><Link href={`/cases/${row.case_slug}`} className="flex min-w-0 items-center gap-4"><span className="grid size-10 shrink-0 place-items-center rounded-lg bg-[var(--accent-soft)] text-[var(--accent)]"><FileText size={17} /></span><span className="min-w-0"><b className="block truncate text-sm">{row.title}</b><span className="mt-1 block text-[10px] text-[var(--ink-faint)]">{row.case_slug} · {new Date(row.updated_at).toLocaleString()} · {row.completed ? "Complete" : row.current_stage}</span></span></Link><button type="button" aria-label={`Delete ${row.title}`} onClick={() => void deleteCaseRun(row.id).then(load).catch((caught) => setError(caught instanceof Error ? caught.message : "Could not remove case."))} className="grid size-9 place-items-center rounded-lg text-[var(--red)] hover:bg-[var(--red-soft)]"><Trash2 size={15} /></button></article>)}{rows.length === 0 && <p className="rounded-xl border border-dashed border-[var(--line)] p-12 text-center text-sm text-[var(--ink-muted)]">No saved cases yet. Open a case, complete a scenario, then choose Save case.</p>}</div></main>; }
