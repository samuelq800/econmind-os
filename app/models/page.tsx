import Link from"next/link";import{ArrowRight}from"lucide-react";import{MODEL_CATALOG}from"@/lib/model-catalog";import{Badge}from"@/components/ui/badge";export default function ModelsPage(){return<div className="min-h-[calc(100vh-4rem)] p-5 sm:p-8 lg:p-10"><div className="max-w-3xl"><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-[var(--accent)]">Model library</p><h1 className="mt-3 text-4xl font-bold tracking-[-.05em] sm:text-5xl">Economic mechanisms, made observable.</h1><p className="mt-5 text-base leading-7 text-[var(--ink-muted)]">Choose a model, change its assumptions, and connect the graph to the economic logic underneath. Every calculation runs instantly in your browser.</p></div><div className="mt-12 divide-y divide-[var(--line)] border-y border-[var(--line)]">{MODEL_CATALOG.map(({slug,title,shortTitle,description,difficulty,concepts,icon:Icon,number})=><Link key={slug} href={slug} className="group grid gap-5 py-7 sm:grid-cols-[60px_1fr_auto] sm:items-center sm:px-4"><span className="text-xs font-bold text-[var(--ink-faint)]">{number}</span><div className="flex gap-4"><span className="hidden size-11 place-items-center rounded-lg border border-[var(--line)] bg-[var(--surface)] text-[var(--accent)] sm:grid"><Icon size={19}/></span><div><div className="flex flex-wrap gap-2"><h2 className="text-lg font-bold">{title}</h2><Badge>{difficulty}</Badge></div><p className="mt-1 text-xs font-semibold text-[var(--ink-faint)]">{shortTitle}</p><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--ink-muted)]">{description}</p><div className="mt-3 flex flex-wrap gap-2 text-[10px] text-[var(--ink-muted)]">{concepts.map(c=><span key={c}>· {c}</span>)}</div></div></div><span className="flex items-center gap-2 text-xs font-bold text-[var(--accent)]">Open <ArrowRight size={15}/></span></Link>)}</div></div>}
